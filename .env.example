# Copy to a real env var in the Vercel dashboard (Project → Settings → Environment Variables).
# NEVER commit a real key. This file is only an example placeholder.
FINNHUB_API_KEY=your_finnhub_api_key_here
